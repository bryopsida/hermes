export default {
  heartbeats: {
    producer: {

    },
    consumer: {
      'auto.commit.interval.ms': 1000,
      'auto.offset.reset': 'earliest'
    }
  },
  jsonData: {
    producer: {

    },
    consumer: {
      'auto.commit.interval.ms': 1000,
      'auto.offset.reset': 'earliest'
    }
  }
}
