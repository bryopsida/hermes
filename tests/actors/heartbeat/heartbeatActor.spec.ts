/* eslint-disable no-undef */
describe('heartbeatActor', () => {
  beforeAll(() => {
    // spin up kafka and zookeeper
  })
  afterAll(() => {
    // clean up test containers
  })
  it('Should act on messages in the heartbeat topic', async () => {
    expect(false).toBeTruthy()
  })
  it('Should cleanup resources when torn down', async () => {
    expect(false).toBeTruthy()
  })
})
