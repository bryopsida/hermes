/* eslint-disable no-undef */
import 'jest'

describe('DataSource.Rest', () => {
  it('can create a data source', async () => {
    expect(true).toBe(false)
  })
  it('can fetch a data source', async () => {
    expect(true).toBe(false)
  })
  it('can delete a data source', async () => {
    expect(true).toBe(false)
  })
  it('can update a data source', async () => {
    expect(true).toBe(false)
  })
})
